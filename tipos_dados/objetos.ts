// Objetos em TypeScript: definição de tipos literais.

let carro: {
    marca: string,
    ano: number,
    placa: string,
    valor: number
};

carro = {
    marca: 'Toyota',
    ano: 2020,
    placa: 'ABC-1234',
    valor: 50000
};

// console.log(carro);
